version: '3.8'
services:
  test-db:
    image: postgres:13
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: test_invoice_management
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data