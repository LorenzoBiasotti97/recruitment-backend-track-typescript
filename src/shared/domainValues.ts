/**
 * Defines the possible roles for a user.
 */
export enum UserRole {
  ADMIN = 'ADMIN',
  VIEWER = 'VIEWER',
}

/**
 * Defines the types of tax profiles available.
 */
export enum TaxProfileType {
  INDIVIDUAL = 'INDIVIDUAL',
  COMPANY = 'COMPANY',
  FREELANCER = 'FREELANCER',
}